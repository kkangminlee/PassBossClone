package org.passorder.boss.presentation.main.other

import android.os.Bundle
import android.view.View
import org.passorder.boss.R
import org.passorder.boss.databinding.FragmentEmptyBinding
import org.passorder.ui.base.BindingFragment

class MenuFragment: BindingFragment<FragmentEmptyBinding>(R.layout.fragment_empty) {
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}